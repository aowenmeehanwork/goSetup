package main

import (
	// "net/http"

	// "github.com/gin-gonic/contrib/static"
	// "github.com/gin-gonic/gin"
	"github.com/aowenmeehanwork/goSetup/server"
)

func main() {

	server.Init()

	// // Setup route group for the API
	// api := router.Group("/")
	// {
	// 	api.GET("/", func(c *gin.Context) {
	// 		c.JSON(http.StatusOK, gin.H {
	// 			"message": "Root",
	// 		})
	// 	})
	// }
}